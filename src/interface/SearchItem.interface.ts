export interface ISearchItem {
    address: string;
    category: string;
    location: {x: number , y: number, z: string};
    neighbourhood: string;
    region: string;
    title: string;
    type: string;
}